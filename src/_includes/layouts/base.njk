<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ title }} - Prodyssey</title>

        <!-- Debug info -->
        {% if eleventy.env.mode == "production" %}
            <!-- ENV: PRODUCTION -->
        {% else %}
            <!-- ENV: {{ eleventy.env.mode or 'NOT SET' }} -->
            <!-- env.ELEVENTY_ENV: {{ process.env.ELEVENTY_ENV or 'NOT SET' }} -->
        {% endif %}

        <!-- Google Analytics 4 (production only) -->
        {% if eleventy.env.mode == "production" or process.env.ELEVENTY_ENV == "production" %}
            <script async src="https://www.googletagmanager.com/gtag/js?id=G-QYH51VNXYM"></script>
            <script>
                window.dataLayer = window.dataLayer || [];
                function gtag() {
                    dataLayer.push(arguments);
                }
                gtag('js', new Date());
                gtag('config', 'G-QYH51VNXYM');
            </script>
        {% endif %}

        {% include "components/seo.njk" %}
        <link rel="stylesheet" href="/assets/css/styles.css">
        <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&family=Merriweather:wght@700&display=swap" rel="stylesheet">
    </head>
    <body class="flex flex-col min-h-screen font-sans text-text bg-background">
        <div class="fixed inset-0 pointer-events-none z-0 opacity-5"></div>
        {% include "components/header.njk" %}

        <main class="flex-grow">
            {{ content | safe }}
        </main>

        {% include "components/footer.njk" %}
    </body>
</html>