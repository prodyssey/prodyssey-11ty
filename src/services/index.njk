---
layout: layouts/base.njk
title: Our Services
---

<section class="py-16 bg-circuit-pattern bg-opacity-5">
    <div class="container mx-auto px-4">
        <h1 class="text-4xl font-serif font-bold text-primary mb-8 text-center">Our Services</h1>
        <p class="text-xl mb-12 text-center">Comprehensive solutions to guide your B2B SaaS journey</p>
        
        {% for pillar in collections.servicePillars | sort(attribute='data.order') %}
        <div class="mb-16">
            <h2 class="text-3xl font-serif font-bold text-secondary mb-6">{{ pillar.data.title }}</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                {% set pillarCategories = collections.serviceCategories | filterByPillar(pillar.data.title) %}
                {% for category in pillarCategories | sort(attribute='data.order') %}
                <div class="bg-white p-6 rounded-lg shadow-md">
                    <h3 class="text-2xl font-serif font-bold text-primary mb-4">{{ category.data.title }}</h3>
                    <p class="mb-4">{{ category.templateContent | safe }}</p>
                    {% set categoryServices = collections.services | filterByCategory(category.data.title) %}
                    <ul class="list-disc list-inside mb-4">
                        {% for service in categoryServices | sort(attribute='data.order') %}
                        <li>{{ service.data.title }}</li>
                        {% endfor %}
                    </ul>
                    <a href="{{ category.url }}" class="text-accent hover:underline">Learn More →</a>
                </div>
                {% endfor %}
            </div>
        </div>
        {% endfor %}
    </div>
</section>