---
layout: layouts/base.njk
title: Prodyssey - Expert help for a profitable SaaS product engine
description: Get clarity, tune up, and generate momentum from your product
image: /assets/images/home-og-image.jpg
---

<section class="text-center py-16 px-4 bg-circuit-pattern bg-opacity-5">
  <div class="container mx-auto max-w-4xl">
    <h1 class="text-5xl font-serif font-bold text-primary mb-4">
      Expert help for a profitable SaaS product engine
    </h1>
    <p class="text-xl mb-8">
      Get clarity, tune up, and start generating more capital efficient momentum.
    </p>
    <a
      href="/services"
      class="bg-accent text-white px-8 py-3 rounded-full hover:bg-secondary transition duration-300"
      >View Services</a
    >
  </div>
</section>

{% include "components/svg-divider.njk" %}

<section class="py-12 px-4">
  <div class="container mx-auto max-w-2xl">
    <div class="flex gap-4 items-baseline">
      <h2 class="text-2xl font-serif font-bold text-primary">Product Engine</h2>
      <span class="text-gray-500 italic">/ˈpraa·duhkt en·jnˈ/</span>
    </div>
    <p class="text-gray-500 italic mb-4">noun</p>
    <div class="pl-6">
      <p class="mb-4">The specialized functions that combine to power SaaS products:</p>
      <p class="text-gray-700">
        <span class="font-semibold">Product</span>,
        <span class="font-semibold">UX</span>,
        <span class="font-semibold">Engineering</span>, 
        <span class="font-semibold">Data</span>, 
        <span class="font-semibold">AI</span>, and 
        <span class="font-semibold">Revenue Enablement</span>
      </p>
    </div>
  </div>
</section>

{% include "components/svg-divider.njk" %}

<section class="py-16 px-4">
  <div class="container mx-auto">
    <div class="grid grid-cols-1 gap-16">
      <!-- Who -->
      <div class="flex flex-col md:flex-row items-center md:items-start md:justify-center gap-8">
        <div class="w-24 h-24 shrink-0">
          <svg class="text-primary w-full h-full" viewBox="0 0 24 24" fill="currentColor">
            <circle cx="12" cy="12" r="10" fill="none" stroke="currentColor" stroke-width="1"/>
            <circle cx="12" cy="12" r="6" fill="none" stroke="currentColor" stroke-width="1"/>
            <circle cx="12" cy="12" r="2" fill="currentColor"/>
          </svg>
        </div>
        <div class="text-center md:text-left max-w-md">
          <h3 class="text-2xl font-serif font-bold text-primary mb-4">Who</h3>
          <p class="text-gray-700">Prodyssey is focused on SaaS companies that have built a good business with ~5 to 75 Million in ARR but are on a growth plateau or nearing one if the product can't create new growth opportunities.</p>
        </div>
      </div>

      <!-- What (reversed layout) -->
      <div class="flex flex-col md:flex-row-reverse items-center md:items-start md:justify-center gap-8">
        <div class="w-24 h-24 shrink-0">
          <img src="/assets/images/prodyssey-logomark-dark.svg" alt="Prodyssey Logo" class="text-primary w-full h-full">
        </div>
        <div class="text-center md:text-right max-w-md">
          <h3 class="text-2xl font-serif font-bold text-primary mb-4">What</h3>
          <p class="text-gray-700">Targeted services to navigate the product problems common to many companies that hit this stage when raising more capital is not a good near term option.</p>
        </div>
      </div>

      <!-- How  -->
      <div class="flex flex-col md:flex-row items-center md:items-start md:justify-center gap-8">
        <div class="w-24 h-24 shrink-0">
          <svg class="text-primary w-full h-full" viewBox="0 0 24 24">
            <!-- Clipboard outline -->
            <path fill="none" stroke="currentColor" stroke-width="1" d="M19 3h-4.18C14.4 1.84 13.3 1 12 1c-1.3 0-2.4.84-2.82 2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2z"/>
            <!-- Clip at top -->
            <circle cx="12" cy="3" r=".5" fill="currentColor"/>
            <!-- Checkmark 1 -->
            <path fill="none" stroke="currentColor" stroke-width="1" d="M7 15l2 2 4-4"/>
            <!-- Checkmark 2 -->
            <path fill="none" stroke="currentColor" stroke-width="1" d="M7 10l2 2 4-4"/>
          </svg>
        </div>
        <div class="text-center md:text-left max-w-md">
          <h3 class="text-2xl font-serif font-bold text-primary mb-4">How</h3>
          <p class="text-gray-700">The Prodyssey Framework - created through deep experience across all key "Product Engine" disciplines during pivots to profitable growth and sustaining it afterwards.</p>
        </div>
      </div>

    </div>
  </div>
</section>

<section
  class="bg-secondary p-8 rounded-lg my-16 bg-greek-pattern bg-opacity-10"
>
  <div class="container mx-auto">
    <h2 class="text-3xl font-serif font-bold mb-4">
      <a href="/framework" class="hover:underline">The Prodyssey Framework</a>
    </h2>
    <p class="text-primary mb-4">
      Our trenches-tested approach to SaaS Product, UX, Engineering, Data, AI, and
      Revenue Enablement for Profitable Growth
    </p>
    <a href="/framework" class="text-accent hover:underline">Learn More →</a>
  </div>
</section>

{% include "components/svg-divider.njk" %}
{% set emailSignup = {
  heading: "Stories from The Journey ",
  subheading: " Join other SaaS leaders receiving the latest updates from our newsletter.",
  buttonText: " Subscribe "
} %}
{%
include "components/email-signup.njk" %}

<section class="my-16 px-4">
  <div class="container mx-auto">
    <h2 class="text-3xl font-serif font-bold text-primary mb-8">
      <a href="/posts" class="hover:underline">Featured Posts</a>
    </h2>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      {% for post in collections.posts | filterByFeatured | reverse | limit(4) %}
        {% include
      "components/blog-post-card.njk" %}
      {% endfor %}
    </div>

    <div class="text-center mt-8">
      <a href="/posts" class="text-accent hover:underline">View All Posts →</a>
    </div>
  </div>
</section>

{% include "components/svg-divider.njk" %}

<section class="my-16 px-4">
  <div class="container mx-auto">
    <a href="/services">
      <h2 class="text-3xl font-serif font-bold text-primary mb-8 text-center">
        Services
      </h2>
    </a>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      {% for pillar in collections.servicePillars | sort(attribute = 'data.order')
 %}

        <div class="bg-white p-6 rounded-lg shadow-md">
          <h3 class="text-2xl font-serif font-bold text-secondary my-2">
            {{ pillar.data.title }}
          </h3>

          <p class="my-4">{{ pillar.templateContent | safe }}</p>

          {% set pillarCategories = collections.serviceCategories | filterByPillar(pillar.data.title) %}
          {% if pillarCategories.length > 0
 %}
            <div class="mt-6">
              {% for category in pillarCategories | sort(attribute = 'data.order') %}
                <h4 class="text-xl font-serif font-bold text-secondary p-4">
                  {{ category.data.title }}
                </h4>

                <ul class="list-disc list-inside mb-2 p-2">
                  {% set categoryServices = collections.services | filterByCategory(category.data.title) %}
                  {% if categoryServices and
                  categoryServices.length > 0 %}
                    {% for service in categoryServices | sort(attribute = 'data.order') %}
                      <li class="my-2">
                        <a href="/services/{{ service.data.slug }}"
                >{{ service.data.title }}</a
              >
                      </li>
                    {% endfor %}
                  {% else %}
                    <li>No services found for category: {{ category.data.title }}</li>
                  {% endif %}
                </ul>
              {% endfor %}
            </div>
          {% else %}
            <p>No categories found for pillar: {{ pillar.data.title }}</p>
          {% endif %}

          <a href="{{ pillar.url }}" class="text-accent hover:underline"
          >Learn More →</a
        >
        </div>
      {% endfor %}
    </div>
  </div>
</section>