---
layout: layouts/base.njk
title: Navigate Your Product Odyssey
description: Prodyssey guides B2B SaaS teams to profitable growth through innovative strategies, deep expertise, and our proprietary PTD4PG framework.
image: /assets/images/home-og-image.jpg
---

<section class="text-center py-16 px-4 bg-circuit-pattern bg-opacity-5">
    <div class="container mx-auto">
        <h1 class="text-5xl font-serif font-bold text-primary mb-4">Navigate Your Product Odyssey</h1>
        <p class="text-xl mb-8">Guiding B2B SaaS teams to profitable growth</p>
        <a href="/contact" class="bg-accent text-white px-8 py-3 rounded-full hover:bg-secondary transition duration-300">Start Your Journey</a>
    </div>
</section>

<section class="my-16 px-4">
    <div class="container mx-auto">
        <h2 class="text-3xl font-serif font-bold text-primary mb-8 text-center">Our Services</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            {% for pillar in collections.servicePillars | sort(attribute='data.order') %}
            <div class="bg-white p-6 rounded-lg shadow-md">
                <h3 class="text-2xl font-serif font-bold text-secondary mb-4">{{ pillar.data.title }}</h3>
                <p class="mb-4">{{ pillar.templateContent | safe }}</p>
                <ul class="list-disc list-inside mb-4">
                    {% set pillarCategories = collections.serviceCategories | filterByPillar(pillar.data.title) %}
                    {% for category in pillarCategories | sort(attribute='data.order') %}
                    <li>{{ category.data.title }}</li>
                    {% endfor %}
                </ul>
                <a href="{{ pillar.url }}" class="text-accent hover:underline">Learn More →</a>
            </div>
            {% endfor %}
        </div>
    </div>
</section>

<section class="bg-secondary text-white p-8 rounded-lg my-16 bg-greek-pattern bg-opacity-10">
    <div class="container mx-auto">
        <h2 class="text-3xl font-serif font-bold mb-4">The PTD4PG Framework</h2>
        <p class="mb-4">Our proprietary approach to Product, Tech, and Data for Profitable Growth</p>
        <a href="/ptd4pg" class="text-accent hover:underline">Learn More →</a>
    </div>
</section>

<section class="my-16 px-4">
    <div class="container mx-auto">
        <h2 class="text-3xl font-serif font-bold text-primary mb-8">Latest Insights</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
            {% for post in collections.posts | reverse | limit(2) %}
                {% include "components/blog-post-card.njk" %}
            {% endfor %}
        </div>
    </div>
</section>

{% include "components/founder-section.njk" %}